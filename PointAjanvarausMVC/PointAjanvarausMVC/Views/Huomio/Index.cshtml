@model IEnumerable<PointAjanvarausMVC.Models.Huomiot>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Sairaudet)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Muut)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Sairaudet)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Muut)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Huomio_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Huomio_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Huomio_ID })
        </td>
    </tr>
}

</table>
